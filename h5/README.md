# N-Admin H5 移动端

N-Admin 的移动端版本，提供简洁的移动端管理界面。

## 功能特性

- 🚀 基于 Next.js 15 和 React 19
- 📱 响应式设计，专为移动端优化
- 🔐 用户登录认证
- 🎨 现代化 UI 设计
- 🌐 调用主项目 API (localhost:3003)

## 开发环境

确保主项目在 `localhost:3003` 运行：

```bash
# 在主项目目录
npm run dev
```

启动 H5 项目：

```bash
# 在 h5 目录
npm install
npm run dev
```

访问 http://localhost:3005

## 项目结构

```
h5/
├── src/
│   ├── app/
│   │   ├── page.tsx           # 首页
│   │   ├── login/page.tsx     # 登录页
│   │   ├── layout.tsx         # 根布局
│   │   └── globals.css        # 全局样式
│   ├── lib/
│   │   ├── api.ts             # API 调用
│   │   └── auth.ts            # 认证管理
│   └── components/            # 组件目录
├── public/                    # 静态资源
└── package.json
```

## API 接口

H5 项目通过 HTTP 请求调用主项目的 API：

- `POST /api/auth/login` - 用户登录
- `GET /api/auth/session` - 获取会话信息
- 其他业务 API 接口

## 技术栈

- **前端框架**: Next.js 15
- **UI 组件**: Tailwind CSS
- **HTTP 客户端**: Axios
- **状态管理**: localStorage
- **开发语言**: TypeScript